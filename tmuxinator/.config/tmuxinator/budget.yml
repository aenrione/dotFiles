# /home/aenrione/.config/tmuxinator/tws.yml

name: budget
root: ~/my-github/budget/
windows:
  - editor: lvim .
  - servers:
      panes:
        - cd budget-tracker-rails/ && redis-server
        - cd budget-tracker-rails/ && bundle exec sidekiq
        - cd budget-tracker-react/ && yarn start
        - cd budget-tracker-rails/ && rails s -b 0.0.0.0
