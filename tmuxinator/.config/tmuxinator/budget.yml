# /home/aenrione/.config/tmuxinator/tws.yml

name: budget
root: ~/my-github/mine/budget/
windows:
  - editor: lvim .
  - servers:
      panes:
        - cd finanzalf/ && yarn start
        - run-android-emulator
